@extends('layouts.app') @section('title','Detail TPS Tool') @section('header','Detail TPS Tool') @section('content')<div class="page-head"><div><h2>{{ $order->order_number }}</h2><span class="badge badge-{{ $order->status }}">{{ $order->status_label }}</span></div></div><div class="card"><div class="detail-grid"><div class="detail-item"><span>Tanggal</span><strong>{{ $order->reported_date->format('d/m/Y') }}</strong></div><div class="detail-item"><span>Tool</span><strong>{{ $order->tool_name }}</strong></div><div class="detail-item"><span>Area</span><strong>{{ $order->area ? $order->area->category.' - '.$order->area->name : '-' }}</strong></div></div><p style="margin-top:18px"><b>Problem</b></p><p class="muted">{{ $order->problem_description }}</p>@if($order->repair_result)<p><b>Hasil Repair</b></p><p class="muted">{{ $order->repair_result }}</p>@endif @if($order->status==='completed')<form method="POST" action="{{ route('user.tps.confirm',$order) }}">@csrf<button class="btn btn-success">Konfirmasi Serah Terima</button></form>@endif</div>@endsection
