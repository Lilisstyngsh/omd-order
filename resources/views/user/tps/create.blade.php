@extends('layouts.app') @section('title','Form TPS Tool') @section('header','Form TPS Tool') @section('content')<div class="card form-card"><form method="POST" action="{{ route('user.tps.store') }}">@csrf<div class="form-grid"><div class="field"><label>Tanggal</label><input type="date" name="reported_date" value="{{ old('reported_date',now()->toDateString()) }}" required></div><div class="field"><label>Area</label><select name="area_id"><option value="">Pilih area (opsional)</option>@foreach($areas as $area)<option value="{{ $area->id }}">{{ $area->category }} - {{ $area->name }}</option>@endforeach</select></div><div class="field full"><label>Nama TPS Tool</label><input name="tool_name" value="{{ old('tool_name') }}" required></div><div class="field full"><label>Problem</label><textarea name="problem_description" required>{{ old('problem_description') }}</textarea></div></div><div style="margin-top:18px"><button class="btn btn-primary">Kirim Form TPS Tool</button></div></form></div>@endsection
